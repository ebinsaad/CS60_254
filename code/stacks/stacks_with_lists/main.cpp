#include <iostream>
#include "stack.h"
#include <stack>

using std::cin;
using std::cout;
using std::endl;

void PrintStackElement(StackElement e){
    cout<<e.value<<endl;
}

int main() {

    /* Self Practice
     *  (1) Create a stack and push 10 elements in it.
     *  (2) Print the stack
     *  (3) Pop 3 elements from the stack
     *  (4) Print the stack
     *  (5) check if stack empty
     *  (6) delete the stack
     *  (7) print the stack
     *
     *  (8) change stack size function to get the size of the stack in O(1)
     *
     *  Identify the pre and post conditions of the stack functions before you use them
     */

    return 0;
}